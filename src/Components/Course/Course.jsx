
const Course = ({course}) => {
    const {image,title,description,price,credit}=course
    return (
        <div>
             <h2>this is course</h2>
        </div>
    );
};

export default Course;